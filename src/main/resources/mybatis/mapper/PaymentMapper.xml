<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.min.cinemagreen.mapper.IPaymentMapper">

  <insert id="insertPay" parameterType="map">
    INSERT INTO payment_t (
       payId,
       amount,
       payDt,
       ticketNo
    ) VALUES (
       #{payId},
       #{amount},
       CURRENT_DATE,
       #{ticketNo}
       
    )
  </insert>
  <select id="getPayinfo"  resultType="PaymentDTO">
    SELECT payId,
       userNo,
       amount,
       ticketNo,
       payDt,
       payState           
      FROM payment_t

  </select>
  
  
</mapper>